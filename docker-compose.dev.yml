version: '3.8'

services:
  web-local:
    build:
      context: .
      target: dev
    ports:
      - "3002:3002"
    restart: unless-stopped
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - PORT=3002
      - NODE_ENV=development
