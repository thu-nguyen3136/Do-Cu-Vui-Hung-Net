version: '3.8'

services:
  web-production:
    build:
      context: .
      target: runner
    ports:
      - "3003:3003"
    restart: unless-stopped
    environment:
      - TZ=Asia/Ho_Chi_Minh
    volumes:
      - ${DEPLOY_PATH:-/tmp}/logs:/var/log/nginx
