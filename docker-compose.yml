version: '3.8'

services:
  web-production:
    build:
      context: .
      target: runner
    ports:
      - "3003:3003"
    restart: unless-stopped
    volumes:
      - ${DEPLOY_PATH:-/tmp}/logs:/var/log/nginx
